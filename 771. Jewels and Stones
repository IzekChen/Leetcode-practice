class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        count = 0
        for i in list(J):
            if S.count(i) != 0:
                count += S.count(i)
        return count